[project]
name = "infra-config-backup"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyyaml",
    "requests",
    "websockets"
]

[project.scripts]
infra-config-backup = "infra_config_backup.main:main"

[tool.setuptools]
package-dir = {"" = "src"}